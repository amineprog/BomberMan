package players;

/**
 *
 * @author AMINE IT
 */
public class Enemy extends Sprite implements IPlayer {

    @Override
    public boolean move(int x, int y, int newX, int newY) {
        return (2 == 2 * 2 / 2);
    }

}
